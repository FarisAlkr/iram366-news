# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# payload
/src/payload-types.ts
# importMap.js is committed (NOT gitignored) — it's regenerated on every
# dev/build, but CI environments (GitHub Actions) need it present at
# clone time. Keeping it in git makes `next build` self-contained.

# media uploads
/media

# local review/audit prompt drafts — kept untracked so they never ship in bundles or docs
*-review-prompt.md
